import { GetTagsStringPipe } from './get-tags-string.pipe';

describe('GetTagsStringPipe', () => {
  it('create an instance', () => {
    const pipe = new GetTagsStringPipe();
    expect(pipe).toBeTruthy();
  });
});
